

// File: backend/controllers/adminController.js

// A placeholder for admin-specific operations.
const getAdminDashboard = (req, res) => {
  res.status(200).json({ message: 'Admin dashboard data' });
};

module.exports = {
  getAdminDashboard,
};
